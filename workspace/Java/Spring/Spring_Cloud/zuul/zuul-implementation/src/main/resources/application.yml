server:
  port: 8080

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

#zuul:
#  routes:
#    books:
#      path: /books/**
#      url: http://localhost:8090
#      strip-prefix: true

spring:
  application:
    name: zuul-gateway